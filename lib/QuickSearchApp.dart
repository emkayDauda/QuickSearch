import 'package:flutter/material.dart';

class QuickSearchApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(
          child: Text('QuickSearch'),
        ),
      ),
      body: Container(
        child: Center(
          child: Text('New App'),
        ),
      ),
    );
  }
}
